#section %h{<%= @item[:name] %>}
  #listing <%= @item[:usage] %>

<%= indent_for_dmark(1, kramdown2dmark(@item[:description] || @item[:summary])) %>

  <% if @item[:aliases] && @item[:aliases].size > 0 %>#p Also known as <%= @item[:aliases].map { |i| "%command{#{i}}" }.join(', ') %>.<% end %>

<%= indent_for_dmark(1, dmark_options_for(@item)) %>
